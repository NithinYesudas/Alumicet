import 'package:flutter/material.dart';
class LastMessage extends StatelessWidget {
  const LastMessage({required this.userId,Key? key}) : super(key: key);
  final String  userId;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
